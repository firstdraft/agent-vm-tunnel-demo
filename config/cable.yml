development:
  adapter: async

test:
  adapter: test

production:
  # This smoke test deliberately has no external service dependency. Use one
  # process; a real multi-process app needs a shared Action Cable adapter.
  adapter: async
